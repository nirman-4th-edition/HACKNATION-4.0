import React from "react";
import ReactDOM from "react-dom";
import App from "./App.jsx"; // Ensure the correct path to App.jsx

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
