// Paste your local IP here and leave the port to be 5000. Or else you won't be able to connect through emulator.
// On windows run ipconfig and paste your ipv4 here from the Wifi adapter section.
String baseURL = "192.168.29.15:5000";

// Order list will be refreshed after this time.
const Duration refreshDuration = Duration(seconds: 20);
